[project]
name = "weatherstation"
version = "0.1.0"
description = "Ecowitt weather aggregator and FTPS uploader"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.2",
]
